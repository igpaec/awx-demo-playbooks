# Dummy inventory for familiarisation only. These hosts are never contacted —
# the demo playbooks use connection: local. Optionally import this file into an
# AWX Inventory (the "Faster alternative" in Part 1 §8.3) instead of adding the
# hosts by hand.

host01
host02
